# Secure File Transfer — Relay Server Dependencies
# Відповідає Таблиці В.3 звіту

# --- Веб-фреймворк ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- Криптографія ---
cryptography>=42.0.0

# --- QR-коди ---
qrcode[pil]>=7.4.2

# --- Утиліти ---
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Тестування ---
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0

# --- Безпека (аналіз) ---
# bandit>=1.7.0
# pip-audit>=2.7.0
